const {assignPublicDid} = require("./assignPublicDid");
const {createDid} = require("./createDid");
const {getPublicDid} = require("./getPublicDid");
const {getDids} = require("./getDids");

module.exports = {
  getDids,
  assignPublicDid,
  createDid,
  getPublicDid
};