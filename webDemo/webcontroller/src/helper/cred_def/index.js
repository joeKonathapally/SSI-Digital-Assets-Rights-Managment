const {createCredDef} = require("./createCredDef");
const {getCredDefs} = require("./getCredDefs");
const {getCredDefId} = require("./getCredDefId");

module.exports = {
  createCredDef,
  getCredDefs,
  getCredDefId
};